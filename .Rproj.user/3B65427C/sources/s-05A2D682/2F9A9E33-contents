#load packages
require(here)
require(dplyr)
require(tibble)
require(hierfstat)

# and data
load(here('FST','fst_hierfstat.rda'))
fst_hierfstat[1:7,1:7]

fst.stats <- basic.stats(fst_hierfstat[,-1:-4],diploid = T,digits = 4)
